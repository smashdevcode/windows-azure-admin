@model IEnumerable<WindowsAzureAdmin.Shared.Storage.BlobStorage.StorageItem>

@{
    ViewBag.Title = "Storage | Containers";
}

@Html.RenderSubMenu("Storage")

<h3>Containers</h3>

@Html.RenderGrid("containers", Model, deleteAction: (item => Url.Action("ContainerDelete", new { containername=item.Name })))
